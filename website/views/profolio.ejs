<!DOCTYPE html>
<html lang="en">
  <% if (head) { %>
    <%- include("components/head", head) %> 
  <% } %>

  <body style="background-color: var(--main-color);" data-aos-easing="ease" data-aos-duration="400">
    <noscript>
      To use this website, you need JavaScript enabled.
    </noscript>

    <div id="loading">
      <div id="loading-center">
        <div id="loading-center-absolute">
          <div class="loader"></div>
        </div>
      </div>
    </div>

    <% if (navagation) { %>
     <%- include("components/navagation", navagation) %> 
    <% } %>

    <% if (top) { %>
     <%- include("components/top", top) %> 
    <% } %>

    <% if (features) { %>
      <%- include("components/features", features) %> 
    <% } %>

    <% if (trusted) { %>
      <%- include("components/trusted", trusted) %> 
    <% } %>

    <% if (footer) { %>
      <%- include("components/footer", footer) %> 
    <% } %>
    
    <%- include("components/scripts") %>
  </body>
</html>
